#!/bin/bash
# Theory Service - Start Script

export PYTHONPATH="${PYTHONPATH}:$(pwd)"
export DATABASE_URL="${DATABASE_URL:-postgresql:///essay_genre_db}"

# Default port
PORT=${PORT:-8000}

# Run with uvicorn
uvicorn api.main:app --host 0.0.0.0 --port $PORT --reload
